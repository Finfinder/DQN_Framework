repos:
  - repo: local
    hooks:
      - id: validate-version-consistency
        name: validate-version-consistency
        entry: python scripts/validate_version_consistency.py --repo-root .
        language: python
        pass_filenames: false